'use client'
import StartMenu from "@/app/components/start-menu";

export default function HomePage() {
    return (
        <div className="bg-custom-gradient min-h-screen">
            <StartMenu/>
        </div>
    );
}